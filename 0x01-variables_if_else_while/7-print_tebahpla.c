#include <stdio.h>

/**
* main - Entry point, prints the lowercase alphabet in reverse
*
* Return: Always 0 (Success)
*/

int main(void)
{
int b;

for (b = 'z'; b >= 'a'; b--)
{
putchar(b);
}
putchar('\n');

Return (0);
}
