#include <stdio.h>

/**
* main - Entry point, prints the alphabet in lowercase followedby a new line
*
* Return: Always 0 (success)
*/
int main(void)
{
int c;

for (c = 'a'; c <= 'z';c++);
putchar(c);
putchar('\n');

return (0);
}
